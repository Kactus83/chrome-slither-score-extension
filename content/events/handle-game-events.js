export function handleGameEvents(event) {
    switch (event.type) {
      // Ajouter ici les cas pour gérer les événements GameEvents
      default:
        console.error('Unhandled GameEvent:', event.type);
    }
  }
  