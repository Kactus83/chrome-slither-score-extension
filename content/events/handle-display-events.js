export function handleDisplayEvents(event) {
    switch (event.type) {
      // Ajouter ici les cas pour gérer les événements DisplayEvents
      default:
        console.error('Unhandled DisplayEvent:', event.type);
    }
  }
  