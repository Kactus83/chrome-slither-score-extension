export function handleUserEvents(event) {
    switch (event.type) {
      // Ajouter ici les cas pour gérer les événements UserEvents
      default:
        console.error('Unhandled UserEvent:', event.type);
    }
  }
  